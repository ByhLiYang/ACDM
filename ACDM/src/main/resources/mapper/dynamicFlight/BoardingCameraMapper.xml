<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.hq.acdm.dao.dynamicFlight.BoardingCameraMapper" >

  <select id="findDeviceNameInfo" resultType="String" parameterType="map" >
      SELECT CAMERA_ID FROM T_BOARDING_CAMERA
      WHERE BOARDING_PORT = #{params.boardingPost,jdbcType=VARCHAR}
  </select>

</mapper>