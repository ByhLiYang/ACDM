<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.hq.acdm.dao.index.VDelayFlightSumMapper" >
  <resultMap id="BaseResultMap" type="com.hq.acdm.model.index.VDelayFlightSum" >
    <result column="FLIGHT_NUM" property="flightNum" jdbcType="DECIMAL" />
    <result column="DELAY_FLIGHT_NUM" property="delayFlightNum" jdbcType="DECIMAL" />
    <result column="PASSENGER_NUM" property="passengerNum" jdbcType="DECIMAL" />
    <result column="TYPE" property="type" jdbcType="VARCHAR" />
  </resultMap>
  <insert id="insert" parameterType="com.hq.acdm.model.index.VDelayFlightSum" >
    insert into V_DELAY_FLIGHT_SUM (FLIGHT_NUM, DELAY_FLIGHT_NUM, PASSENGER_NUM, 
      TYPE)
    values (#{flightNum,jdbcType=DECIMAL}, #{delayFlightNum,jdbcType=DECIMAL}, #{passengerNum,jdbcType=DECIMAL}, 
      #{type,jdbcType=VARCHAR})
  </insert>
  <insert id="insertSelective" parameterType="com.hq.acdm.model.index.VDelayFlightSum" >
    insert into V_DELAY_FLIGHT_SUM
    <trim prefix="(" suffix=")" suffixOverrides="," >
      <if test="flightNum != null" >
        FLIGHT_NUM,
      </if>
      <if test="delayFlightNum != null" >
        DELAY_FLIGHT_NUM,
      </if>
      <if test="passengerNum != null" >
        PASSENGER_NUM,
      </if>
      <if test="type != null" >
        TYPE,
      </if>
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides="," >
      <if test="flightNum != null" >
        #{flightNum,jdbcType=DECIMAL},
      </if>
      <if test="delayFlightNum != null" >
        #{delayFlightNum,jdbcType=DECIMAL},
      </if>
      <if test="passengerNum != null" >
        #{passengerNum,jdbcType=DECIMAL},
      </if>
      <if test="type != null" >
        #{type,jdbcType=VARCHAR},
      </if>
    </trim>
  </insert>

  <select id="getRecord" resultMap="BaseResultMap">
    select * from V_DELAY_FLIGHT_SUM
  </select>

  <select id="getDelayTotal" resultType="java.lang.Integer">
    select COUNT(*) total from V_DELAY_FLI_NO
  </select>

  <select id="getDateName" resultType="com.hq.acdm.vo.supervision.managermonitor.FourD">
    select RWYBYA d0,RWYBYD d1 from V_DAILY_RUNWAY
  </select>
</mapper>