<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.hq.acdm.dao.effectAnalysis.TTaxitimeVarReportMapper" >

  <select id="findTTaxitimeVarInfo" resultType="com.hq.acdm.vo.effectAnalysis.TTaxitimeVarReportVo" parameterType="map" >
    SELECT ID id,
    RUNWAY runway,
    STAND stand,
    CRAFT_TYPE_ICAO craftTypeIcao,
    TAXI_TIME taxiTime,
    D_OR_A dora,
    PERIOD period
    FROM T_TAXITIME_VAR
    WHERE 1=1
    <if test="params.runway !=null and params.runway != ''">
      AND RUNWAY = #{params.runway,jdbcType=VARCHAR}
    </if>
    <if test="params.stand !=null and params.stand != ''">
      AND STAND = #{params.stand,jdbcType=VARCHAR}
    </if>
    <if test="params.craftTypeIcao !=null and params.craftTypeIcao != ''">
      AND CRAFT_TYPE_ICAO = #{params.craftTypeIcao,jdbcType=VARCHAR}
    </if>
    <if test="params.dora !=null and params.dora != ''">
      AND D_OR_A = #{params.dora,jdbcType=VARCHAR}
    </if>
    <if test="params.period !=null and params.period != ''">
      AND PERIOD = #{params.period,jdbcType=VARCHAR}
    </if>
  </select>

  <select id="findAllRunway" resultType="string"  >
    SELECT DISTINCT RUNWAY FROM T_TAXITIME_VAR
    WHERE RUNWAY IS NOT NULL
    ORDER BY RUNWAY ASC
  </select>

  <select id="findAllStand" resultType="string"  >
    SELECT DISTINCT STAND FROM T_TAXITIME_VAR
    WHERE STAND IS NOT NULL
    ORDER BY STAND ASC
  </select>

  <select id="findAllCraftTypeIcao" resultType="string"  >
    SELECT DISTINCT CRAFT_TYPE_ICAO FROM T_TAXITIME_VAR
    WHERE CRAFT_TYPE_ICAO IS NOT NULL
    ORDER BY CRAFT_TYPE_ICAO ASC
  </select>

  <select id="findAllPeriod" resultType="string"  >
    SELECT DISTINCT PERIOD FROM T_TAXITIME_VAR
    WHERE PERIOD IS NOT NULL
    ORDER BY PERIOD ASC
  </select>

</mapper>